apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: llm-models
spec:
  storageClassName: balanced-storage
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 50Gi